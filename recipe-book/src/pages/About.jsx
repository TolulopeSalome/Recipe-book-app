import React from 'react'

const About = (props) => {
  console.log(props);
  

  return (
    <>
    <div>practicepage {props.name} </div>
    <div>practicepage {props.name} </div>
    <div>practicepage {props.name} </div>
    <div>practicepage {props.name} </div>
    <div>practicepage {props.name} </div>
    <div>practicepage {props.name} </div>
    <div>practicepage {props.name} </div>
    <div>practicepage {props.name} </div>
    </>

    
  )
}

export default About